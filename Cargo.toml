[workspace]

members = [
    "contracts/credential",
    "contracts/reputation",
    "contracts/governance",
]

[profile.release]
opt-level = "z"
overflow-checks = true
debug = false
panic = "abort"
codegen-units = 1
